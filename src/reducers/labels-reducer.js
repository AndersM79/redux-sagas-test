import initialState from '../constants/state/labels-initial-state';

const labelsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default labelsReducer;
